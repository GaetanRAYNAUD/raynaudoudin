#include "network.hpp"

#include "server.h"

namespace server {
    void network_Test() {
        try {

        } catch(std::exception& e) {
            std::cerr << "Exception: " << e.what() << std::endl;
        }
    }
}
